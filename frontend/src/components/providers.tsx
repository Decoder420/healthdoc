"use client";

/**
 * Legacy alias — prefer importing Providers from `@/providers`.
 * Kept so older imports keep working without @mui/material-nextjs.
 */
export { Providers, Providers as AppProviders } from "@/providers";
